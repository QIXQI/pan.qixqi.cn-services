spring:
  application:
    name: userservice
  profiles:
    active: default
  cloud:
    config:
      uri: http://ali6.qixqi.cn:8000
      name: ${spring.application.name}
      profile: ${spring.profiles.active}
      label: master
      enabled: true

# 部署在服务器中，需要指定公网IP
eureka:
  instance:
    ip-address: ali.qixqi.cn
